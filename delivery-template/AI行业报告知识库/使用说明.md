# AI 行业报告知识库

1. 第一次使用 AI 问答前，双击 `配置DeepSeek.command`，按提示粘贴 API Key。
2. 双击 `启动知识库.command`。终端会保持运行，并自动打开浏览器。
3. 关闭启动终端即可停止本地服务。

不配置 Key 时，文章浏览、筛选、中文全文、英文原文和官方链接仍可正常使用；只有生成式问答不可用。

默认模型为 `deepseek-v4-flash`。如需 Pro，可在 `.env.local` 中把 `DEEPSEEK_MODEL` 改为 `deepseek-v4-pro`。

API Key 只保存在本文件夹的 `.env.local`，由本机 Node 服务读取，不会发送到浏览器。请勿分享该文件。
